

// create reducer
const AuthReduser = (authstate, {type, payload}) => {

    switch (type) {
        case '':
            
            break;
    
        default:
            return authstate;
            break;
    }
}

// export Reduser
export default AuthReduser